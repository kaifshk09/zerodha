# TODO

- [x] Inspect repo (Backend/index.js) and identify duplicated/stray route handlers.
- [ ] Remove duplicate route blocks in `Backend/index.js` for: /api/portfolio, /api/holdings, /api/positions, /api/watchlist.
- [ ] Fix any stray corrupted code blocks/braces around the positions/holdings routes.
- [ ] Sanity-check server starts (node/npm) and verify routes return responses.

